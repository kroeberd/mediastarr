apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mediastarr

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # - secret.yaml    # uncomment after filling in credentials
  # - ingress.yaml   # uncomment if using an Ingress controller

# To pin a specific version instead of 'latest':
# images:
#   - name: ghcr.io/kroeberd/mediastarr
#     newTag: "7.1.7"

# To override the timezone:
# patches:
#   - patch: |-
#       - op: replace
#         path: /spec/template/spec/containers/0/env/1/value
#         value: "Europe/Berlin"
#     target:
#       kind: Deployment
#       name: mediastarr
